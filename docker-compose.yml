version: '3'
services:
  app:
    build:
      context: .
      dockerfile: .docker/php/Dockerfile
    image: foodunit
    ports:
    - 8080:80
    volumes:
    - .:/srv/app/public
    links:
    - mysql
  mysql:
    image: mysql:5.7
    environment:
      MYSQL_DATABASE: foodunit2
      MYSQL_USER: foodunit2
      MYSQL_PASSWORD: foodunit2
      MYSQL_ROOT_PASSWORD: foodunit2
